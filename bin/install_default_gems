#!/bin/sh

# install useful gems we want on all rubies
gem install bundler terminal-notifier foreman lolcommits awesome_print pry pry-doc pry-nav cocoapods --no-ri --no-rdoc
gem install seeing_is_believing --version 3.0.0.beta.7
