#!/usr/bin/env bash
set -e

################################################################################
# common .envrc - see `man direnv-stdlib`
################################################################################

# add utils to PATH
PATH_add ~/bin

# aws-vault should use login keychain
export AWS_VAULT_KEYCHAIN_NAME=login
